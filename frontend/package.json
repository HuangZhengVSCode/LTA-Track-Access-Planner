{
  "name": "lta-track-access-planner",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "prepare:engine": "node scripts/prepare-engine.mjs",
    "dev": "npm run prepare:engine && vite",
    "build": "node scripts/validate-deployment.mjs && npm run prepare:engine && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "lucide-react": "^0.468.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11"
  }
}
